name: VKS Test
on:
  push:
    branches:
    - master
jobs:  
  build:
    runs-on: ubuntu-latest
    
    steps:
 
    - name: Setup python
      uses: actions/setup-python@v1 
      with:
        python-version: '3.x'
        architecture: 'x64' 

    - name: VKS test
      uses: olegchorny/javascript-actions@master
      with: 
        url: ${{ secrets.URL }}
        project: ${{ secrets.PROJECT }}
        domain: ${{ secrets.DOMAIN }}
        user: ${{ secrets.USER }}
        userdomain: ${{ secrets.USERDOMAIN }}
        password: ${{ secrets.PASSWORD }}


